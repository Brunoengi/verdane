{
  "name": "verdane",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "turbo": "latest",
    "prettier": "^3.3.0"
  },
  "packageManager": "pnpm@11.14.0",
  "engines": {
    "node": ">=20"
  }
}
